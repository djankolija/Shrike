# v6.1 implementation plan — reasoning retention

Status of record for [v6.1-reasoning-retention.md](v6.1-reasoning-retention.md).
Checkboxes here are the only status tracking.

Protocol per task: five gates per commit; the settle no-op is verified
live on the mini (settle lines absent from ornith traffic, rig wall
−~1 s/request, no join stalls on warm cards).

## Tasks

- [ ] **R0: policy type + configuration** — `reasoning-retention`
      setting (`as-generated` default | `stripped`), plumbed to the
      render layer following the existing `SHRIKE_*`/flag conventions.
- [ ] **R1: as-generated history render (thinking-off)** — inject the
      empty think block at all three render sites via the shared
      renderer; render must reproduce KV bytes exactly.
- [ ] **R2: dialect force** — Harmony forces `stripped` regardless of
      configuration (structural: its template cannot render analysis in
      history).
- [ ] **R3: tests** — render golden bytes (block injected under
      as-generated + thinking-off), settle no-op assertion
      (settled == kv → unchanged), Harmony forced-strip arm, config
      plumbing.
- [ ] **R4: deploy + live verdict** — settle lines gone from ornith
      traffic; rig wall delta; warm-card turn without join stall;
      one-time cache invalidation observed and noted.
