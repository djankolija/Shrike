# v6.1 implementation plan — reasoning retention

Status of record for [v6.1-reasoning-retention.md](v6.1-reasoning-retention.md).
Checkboxes here are the only status tracking.

Protocol per task: five gates per commit; the settle no-op is verified
live on the mini (settle lines absent from ornith traffic, rig wall
−~1 s/request, no join stalls on warm cards).

## Tasks

- [x] **R0: policy type + configuration** — LANDED c796fc3:
      `ReasoningRetention` beside its v7 sibling, SHRIKE_REASONING_RETENTION
      env + --reasoning-retention flag (validated), plumbed
      ServerArguments → ModelRegistry → ModelSessionPlan → session
      load, banner extended.
- [x] **R1: as-generated history render** — LANDED c796fc3 as an
      idempotent message transform applied at the four render entries
      (not inside the four leaf renderers — no Jinja surgery): echoed
      `thinking:` verbatim in its block, empty block under
      thinking-off, pass-through when a thinking-on client did not
      echo (degrades to LCP rewind).
- [x] **R2: dialect force** — LANDED c796fc3: Harmony forces
      `stripped` at session resolve (stderr warning on explicit
      override attempts); the transform's dialect guard passes Harmony
      and Kimi through untouched.
- [x] **R3: tests** — LANDED c796fc3: six arms (golden bytes retained
      vs stripped, idempotency, echoed-thinking verbatim, thinking-on
      no-echo no-op, settled-live-region KV-form extension, Harmony
      no-op). Suite 1092 → 1098.
- [x] **R4: deploy + live verdict — CONFIRMED 2026-09-01 ~12:15
      (arm3/arm2 session + real-conversation probe).** The settle on
      ornith/thinking-off degenerated to a **2-token forward
      extension**: `kind=settle rewind=30 kv=30 kv_tail=[]
      settled_tail=[248046, 198]` (im_end + \n) followed by
      settle_rewind — no reconstruction, no re-prefill, and turn 2
      served cached=32/50 (the block-injected render matched KV
      byte-for-byte). Rig digest exact (494bab3edb62), wait/body
      unchanged vs pre-v6.1 (38.59/39.88 vs 38.38/39.69 — loop
      untouched as designed); ladder prompts grew +4/turn as expected
      (1900 → 1912). The drop_emission path (canned-replay shape:
      client never echoes the answer) still restores via snapshot —
      correct and cheap, steady state. Live trial: v6.1 binary,
      PID 47716.
